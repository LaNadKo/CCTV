services:
  db:
    image: postgres:16-alpine
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cctv}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cctv}
      POSTGRES_DB: ${POSTGRES_DB:-cctv}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cctv}"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  backend:
    build: .
    ports:
      - "8000:8000"
      - "5005:5005/udp"
    env_file: .env
    environment:
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER:-cctv}:${POSTGRES_PASSWORD:-cctv}@db:5432/${POSTGRES_DB:-cctv}"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - recordings:/app/recordings
      - snapshots:/app/snapshots

  ruview-sensing:
    image: ruvnet/wifi-densepose:latest
    container_name: ruview-sensing
    environment:
      CSI_SOURCE: ${RUVIEW_SIDECAR_SOURCE:-simulated}
      RUST_LOG: ${RUVIEW_SIDECAR_LOG_LEVEL:-info}
      MODELS_DIR: ${RUVIEW_SIDECAR_MODELS_DIR:-data/models}
    ports:
      - "${RUVIEW_SIDECAR_HTTP_PORT:-3100}:3000"
      - "${RUVIEW_SIDECAR_WS_PORT:-3101}:3001"
      - "${RUVIEW_SIDECAR_UDP_PORT:-5505}:5005/udp"
    restart: unless-stopped
    profiles:
      - ruview

  processor:
    build: ./processor
    environment:
      BACKEND_URL: "http://backend:8000"
      API_KEY: "${PROCESSOR_API_KEY:-processor-secret-key-2026}"
      PROCESSOR_NAME: "processor-1"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    profiles:
      - with-gpu

  mediamtx:
    image: bluenviron/mediamtx:latest
    ports:
      - "8554:8554"
      - "1935:1935"
      - "8888:8888"
    environment:
      MTX_PROTOCOLS: tcp
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    environment:
      VITE_API_URL: "http://${DOMAIN:-localhost}:8000"
    depends_on:
      - backend
    profiles:
      - with-frontend

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/default.conf.template:/etc/nginx/templates/default.conf.template:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    environment:
      DOMAIN: ${DOMAIN:-localhost}
    depends_on:
      - backend
      - frontend
    restart: unless-stopped
    profiles:
      - with-nginx

volumes:
  pgdata:
  recordings:
  snapshots:
